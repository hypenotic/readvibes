# Read Fortunes Environment Variables
# Copy this to .env and fill in your values

# Anthropic API Key (required for generating readings)
ANTHROPIC_API_KEY=your_api_key_here

# Resend API Key (required for emailing readings)
# Sign up at https://resend.com and create an API key
RESEND_API_KEY=your_resend_api_key_here

# Resend From Email (required for delivery to real users)
# ──────────────────────────────────────────────────────
# The default sandbox sender (onboarding@resend.dev) only delivers to the
# Resend account owner's email. To send to anyone else you MUST:
#   1. Add your domain in Resend → Dashboard → Domains
#   2. Add the DNS records (MX, SPF, DKIM) to your domain registrar
#   3. Wait for Resend to verify the domain
#   4. Set this variable to your verified sender, e.g.:
#
# RESEND_FROM_EMAIL=Read Fortunes <hello@readfortune.es>
#
# If left blank, the app falls back to onboarding@resend.dev (dev/testing only).
RESEND_FROM_EMAIL=
